// test_types.dryad - Valid code
let x: number = 10;
const Y: string = "Hello";
let b: bool = true;

function add(a: number, b: number): number {
    return a + b;
}

let result: number = add(x, 5);
print(result);
print(Y);

// test_types_invalid.dryad - Invalid code (uncomment to test individually)
// let z: number = "not a number";
// const W: bool = 123;
// function greet(name: string): number { return name; }
