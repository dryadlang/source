#<console_io>
#<time>
#<system_env>
#<encode_decode>

// ============================================
// TESTE COMPLETO DOS MÓDULOS IMPLEMENTADOS
// ============================================

native_println("=== 🚀 DEMONSTRAÇÃO DOS MÓDULOS NATIVOS ===");
native_println("");

// ============================================
// 1. MÓDULO CONSOLE_IO - Entrada e saída
// ============================================
native_println("📝 1. TESTANDO MÓDULO CONSOLE_IO:");

// Input básico
native_println("Digite seu nome:");
var nome = native_input();
native_println("Olá, " + nome + "!");

// Input com timeout
native_println("Digite algo em 3 segundos (ou será timeout):");
var resposta_timeout = native_input_timeout(3);
se (resposta_timeout == null) {
    native_println("⏰ Timeout! Muito devagar...");
} senao {
    native_println("✅ Você digitou: " + resposta_timeout);
}

// Print colorido
native_print("🎨 Texto colorido!", "green");
native_print("🔥 Texto vermelho!", "red");

native_println("");

// ============================================
// 2. MÓDULO TIME - Tempo e data
// ============================================
native_println("⏰ 2. TESTANDO MÓDULO TIME:");

var timestamp_atual = native_timestamp();
native_println("⏱️ Timestamp atual: " + timestamp_atual);

var data_atual = native_date();
native_println("📅 Data atual: " + data_atual);

var hora_atual = native_time();
native_println("🕐 Hora atual: " + hora_atual);

var timestamp_ms = native_timestamp_ms();
native_println("⏱️ Timestamp em milissegundos: " + timestamp_ms);

var data_formatada = native_format_date(timestamp_atual, "%d/%m/%Y %H:%M:%S");
native_println("✨ Data formatada: " + data_formatada);

native_println("⏳ Dormindo por 2 segundos...");
native_sleep(2);
native_println("😴 Acordei!");

var nova_data = native_parse_date("2024-12-25 10:30:00", "%Y-%m-%d %H:%M:%S");
native_println("🎄 Data do Natal 2024 (timestamp): " + nova_data);

native_println("");

// ============================================
// 3. MÓDULO SYSTEM_ENV - Sistema e ambiente
// ============================================
native_println("💻 3. TESTANDO MÓDULO SYSTEM_ENV:");

var sistema = native_os_name();
native_println("🖥️ Sistema operacional: " + sistema);

var arquitetura = native_arch();
native_println("🏗️ Arquitetura: " + arquitetura);

var usuario = native_username();
native_println("👤 Usuário atual: " + usuario);

var hostname = native_hostname();
native_println("🌐 Nome do host: " + hostname);

var dir_atual = native_current_dir();
native_println("📁 Diretório atual: " + dir_atual);

var dir_home = native_home_dir();
native_println("🏠 Diretório home: " + dir_home);

var dir_temp = native_temp_dir();
native_println("🗂️ Diretório temporário: " + dir_temp);

// Teste de variável de ambiente
var path_env = native_get_env("PATH");
se (path_env != null) {
    native_println("🛤️ PATH encontrado (primeiros 100 chars): " + native_substring(path_env, 0, 100));
} senao {
    native_println("❌ PATH não encontrado");
}

native_println("");

// ============================================
// 4. MÓDULO ENCODE_DECODE - Conversão de dados
// ============================================
native_println("🔄 4. TESTANDO MÓDULO ENCODE_DECODE:");

// Teste JSON
var dados_teste = {"nome": "João", "idade": 30, "ativo": true};
var json_string = native_json_encode(dados_teste);
native_println("📄 JSON codificado: " + json_string);

var dados_decodificados = native_json_decode(json_string);
native_println("✅ JSON decodificado com sucesso!");

// Teste CSV
var dados_csv = [["Nome", "Idade", "Cidade"], ["Ana", "25", "São Paulo"], ["Carlos", "35", "Rio de Janeiro"]];
var csv_string = native_csv_encode(dados_csv);
native_println("📊 CSV codificado:");
native_println(csv_string);

var csv_decodificado = native_csv_decode(csv_string);
native_println("✅ CSV decodificado com " + csv_decodificado.length + " linhas");

// Teste XML
var dados_xml = {"produto": {"nome": "Notebook", "preco": 2500, "disponivel": true}};
var xml_string = native_xml_encode(dados_xml, "catalogo");
native_println("📋 XML codificado: " + xml_string);

var xml_decodificado = native_xml_decode(xml_string);
native_println("✅ XML decodificado com sucesso!");

native_println("");

// ============================================
// 5. RESUMO E CONCLUSÃO
// ============================================
native_println("📊 === RESUMO DOS MÓDULOS TESTADOS ===");
native_println("✅ CONSOLE_IO: Input/Output avançado - 8 funções");
native_println("✅ TIME: Manipulação de tempo e data - 7 funções");  
native_println("✅ SYSTEM_ENV: Informações do sistema - 8 funções");
native_println("✅ ENCODE_DECODE: JSON/CSV/XML - 6 funções");
native_println("");
native_println("🎯 TOTAL: 29 funções nativas implementadas!");
native_println("🚀 Sistema modular funcionando perfeitamente!");

native_println("");
native_println("=== 🎉 DEMONSTRAÇÃO CONCLUÍDA ===");
